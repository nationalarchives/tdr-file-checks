s3 {
    endpoint = "http://localhost:8003/"
    multipart.part.size.bytes = 8388608
}

s3files.mount.point = "./src/test/resources/testfiles"
# Set between the size of ten_bytes (190B) and Test.docx (6118B) so both the download and S3 mount paths are covered
s3files.large.file.threshold.bytes = 200

checksum.buffer.size.bytes = 8388608
poll_malware_scan_complete.await.secs = 5

root.directory="./src/test/resources/testfiles/running-files"

droid.signature.name="DROID_SignatureFile_V"
containers.signature.name="container-signature-"

droid.version="120"
containers.version="20240715"
